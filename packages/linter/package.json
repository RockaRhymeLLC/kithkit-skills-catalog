{
  "name": "@kithkit/linter",
  "version": "0.1.0",
  "description": "Kithkit skill linter — validates skill packages for structure, security, and naming",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "kithkit-lint": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test --experimental-strip-types --no-warnings 'tests/**/*.test.ts'",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "semver": "^7.6.0",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/semver": "^7.5.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
