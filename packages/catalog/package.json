{
  "name": "@kithkit/catalog",
  "version": "0.1.0",
  "description": "Kithkit skill catalog — index builder, archive manager, and CI tooling",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "kithkit-catalog": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test --experimental-strip-types --no-warnings 'tests/**/*.test.ts'",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
